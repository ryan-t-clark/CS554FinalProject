const userData = require('./users');
const gamesData = require('./games');
const picksData = require('./picks');

module.exports = {
    users : userData,
    games : gamesData,
    picks : picksData
}